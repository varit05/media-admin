type Query {
  programs: [Program]
  program(id: ID!): Program
}

type Program {
  id: ID!
  title: String
  description: String
  thumbnail: String
  media: [Media]
}

type Media {
    type: String
    url: String
}
